/** localStorage key for Zustand `persist` on `useAtcStore` — keep in sync with store middleware `name`. */
export const CAPACITY_ATC_PERSIST_KEY = 'capacity_atc';
export const CAPACITY_ATC_PERSIST_VERSION = 1;
