/** Written by `scripts/write-build-meta.mjs` (postinstall, prebuild, dev). */
export {
  APP_VERSION,
  BUILD_TIME_ISO,
  GIT_COMMIT_SHORT,
  LANDING_BOM_AUTH,
  LANDING_BOM_CLIENT,
  LANDING_BOM_FLAGS,
  LANDING_BOM_HOSTING,
} from './buildMeta.generated';
