/** Written by `scripts/write-build-meta.mjs` (postinstall, prebuild, dev). */
export {
  APP_VERSION,
  BUILD_TIME_ISO,
  GIT_COMMIT_MESSAGE,
  GIT_COMMIT_SHA,
  GIT_COMMIT_SHORT,
  GIT_REPO_URL,
  LANDING_BOM_ROWS,
} from './buildMeta.generated';
